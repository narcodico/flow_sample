import 'package:flutter/material.dart';

class LoginFlow extends StatelessWidget {
  const LoginFlow._({Key? key}) : super(key: key);

  static Page page() => const MaterialPage<void>(child: LoginFlow._());

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
